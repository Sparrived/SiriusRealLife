# AGENTS.md

本项目（SiriusRealLife）的工程约定。所有人和 agent 在此仓库工作时都必须遵守本文档。
约定与代码冲突时，改代码。

**本文只放两样东西**：必须时刻生效的硬规则，和文档索引。背景、理由、实施细则都在 [`docs/`](docs/)。
改约定就改文档，别只改代码。

---

## 1. 这是什么

一个人格模拟器：人格 = **有限状态机 + 意识流**。

- 任一时刻只持有**一个**状态
- 状态由 **LLM 决定**：框架给出事实与硬约束，模型自己选换不换、换去哪、待多久
- 状态 = 一段有明确出口的过程：目标 + 信息可见性（订阅哪些频道）+ 退出条件
- 时间由固定 **tick** 驱动。**框架里没有随机**——想要"不作为"就显式选择等待

技术栈：**Go** 后端 + **Vue 3** 前端。LLM 全部经 AMKR。

---

## 2. 硬规则

违反这些会直接出 bug。理由与实施细则见对应文档。

**R1. 一个 agent 一个 goroutine，状态只被它自己改。**
事件从 channel 进，tick 循环用 `select` 收。其他 goroutine 想影响 agent，只能投递事件，**不准**持锁直接改状态。违反这条会出一类极难复现的竞态。

**R2. 状态与心境正交，不合并。**
状态是离散枚举、有出口条件；心境是连续量（精力/烦躁/好奇/亲密度），自己按时间衰减。
影响方向**单向**：心境 → 状态自己的 `guard`/`until`、心境 → prompt 与工具参数；状态 → 只能改心境和意识流。
心境只提供**事实与建议**（"我累了"），不替模型做决定 —— 累不累要不要歇，是模型看到这条事实后自己选。
**不准**把情绪写进状态名（`ANGRY_WORKING` 是错的）。→ [architecture.md](docs/architecture.md)

**R3. 状态完全由 LLM 决定，框架只做约束，不含随机。**
每个状态声明 `guard`/`cooldown`/`minTick`/`maxTick`/`until`，**没有权重**；框架不做加权抽取，也不掷骰子。模型通过调用工具表达决定：
- `enter_state(state, for_ticks, why)` —— 换到另一个状态
- `stay(why, for_ticks)` —— **什么都不做**，只把下次询问推后（"等待 N tick"就是它）

`for_ticks` 的语义是"下次什么时候再问我"，框架把它**夹进** `[minTick, maxTick]`（夹紧不是抽样）。并且：
- 同一状态不允许连续进入两次 —— 当前状态不进候选清单，想继续就用 `stay`
- 硬约束（`guard` / 冷却 / 当前状态）写进工具 schema 的 `enum`，并在校验里兜底，**不靠 prompt 里写"请不要选某某"**
- 一次决定失败（调用失败、没调工具、选了不可进的状态）一律降级为 `stay`，**绝不用随机兜底**
- 可复现性来自**决策日志**（模型给的理由 + 当时的完整候选清单），不来自种子 —— 因此没有 `Seed`，也不用 `math/rand`

**R4. LLM 调用是阻塞 IO，绝不能挡住 tick。**
进行中的调用是一个**显式状态**（`thinking`）。调用期间 agent 照常收事件、照常泵入订阅的消息、**继续待在原状态**（"状态是一段订阅，不是一次跳跃"），且必须能被 `context` 取消。禁止在 tick 循环里同步等一次 30 秒的调用。一次只允许一个调用在途，**决策优先于独白**。

**R5. 意识流必须有界。**
分层存储，各层各有边界；**prompt 只读 `working` + 长期层，绝不塞全量历史**。任何往 prompt 里塞全量历史的代码都是 bug。
意识流是**带类型的日志**（观察/想法/动作/打算），送进 LLM 的是按调用点从日志装配出的**视图**——两者必须分开，日志要全、视图要有界。
层结构、遗忘机制与装配规则见 [memory.md](docs/memory.md)（该文档是分层定义的唯一来源）。

**R6. 每次状态决定都留下结构化日志。**
`from`、`to`、`reason`（`llm`/`stay`）、模型给的 `why`、夹紧后的 `for_ticks`、**当时完整的候选清单（含被挡掉的及原因）**。
没有随机数要记 —— 它是唯一的调试器，也是回归测试的输入。用 `log/slog`，字段不用字符串拼。

**R7. 工具签名统一。**
```go
type Tool func(ctx context.Context, args json.RawMessage) (json.RawMessage, error)
```
工具只是给 LLM 的能力白名单，**不准**在工具里写业务逻辑或状态机逻辑。禁止工具类继承、插件注册表、反射发现。
状态声明的是**信息可见性**与**建议动作**，不是工具白名单；只在有明确安全/一致性理由时才硬封锁具体工具。→ [memory.md](docs/memory.md) §7.1

**R8. 时间是一等公民。**
业务逻辑只认 tick 序号，不认 `time.Now()`。真实时间与游戏时间的换算**只在 tick 源头做一次**。这样离线推演、加速、回放全都免费。

**R9. LLM 只有一个出口：AMKR。**
Sirius 不直接调用任何模型供应商，不引入供应商 SDK，代码里不出现真实模型名（用 `TASK_XXXXXX` 任务名）。→ [llm-amkr.md](docs/llm-amkr.md)

**R10. Sirius 不重试 LLM 调用。**
重试、切 Key、冷却全部由 AMKR 负责。客户端重试 = 双倍计费 + 日志噪音。

**R11. 决策有多个入口，但只有一个决策点。**
"为什么现在问你"的来源有若干种：检查点到点（`dwell`）、状态自己的 `until` 成立、硬上限兜底（`max`），将来还会有更多。**每加一个入口，只准往 `pending` 里追加一条理由，不准各写一条控制流去改状态** —— 状态只在 `applyDecision` 那一处、由模型的选择改变。
这是加入口不炸状态机的唯一原因：入口只提供理由，不拥有决定权。

**R12. 消息到达本身绝不触发决策。**
外部消息只有两件事可做：**(a)** 喂给订阅了该频道的状态（`Pump`），**(b)** 更新计数与条件状态（未读数、烦躁）。控制流里**不准**出现"因为来了一条消息，所以去决定/换状态"。
`@` 与回复**不是**抢占信号，只是"这条未读更显眼"。真人不会因为手机响一声就放下手里的事 —— 但她在看手机时，消息会即时呈现在眼前。想换不换状态，等下一次 `pending` 里的理由成立时由模型决定。

---

## 3. 文档索引

| 文档 | 内容 |
|---|---|
| [docs/architecture.md](docs/architecture.md) | 状态/心境/决策点/tick/意识流五要素、数据流、目录结构 |
| [docs/memory.md](docs/memory.md) | 记忆与注意力模型：QQ 门控、四层记忆、打捞、遗忘/Shadow |
| [docs/conventions.md](docs/conventions.md) | Go / Vue / API 编码约定、提交规范、文档规范 |
| [docs/llm-amkr.md](docs/llm-amkr.md) | AMKR 接入全部细则：客户端、任务路由、超时、WebUI 反代、部署 |
| [docs/roadmap.md](docs/roadmap.md) | 反目标、MVP 范围与验收标准、待定项 |
| [docs/prior-art.md](docs/prior-art.md) | 同类项目调研：可借鉴的检索/反思/裁决逻辑、已知失败模式 |

---

## 4. 不可协商的两条安全线

1. **本仓库已公开。** 提交前自查：没有上游 key、没有 AMKR 的 `local_api_key`、没有 `router-config.json`。
2. **Sirius 自身具备鉴权之前，服务只能绑 `127.0.0.1`。** `/amkr/` 反代等同于 AMKR 的完整管理权限。
