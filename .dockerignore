# 构建上下文排除项：缩小上下文、避免把本机密钥与产物打进镜像。
#
# 最重要的一条：.env 里有 AMKR_API_KEY，绝不能进镜像层
# （镜像层即使后续删除文件也仍可被提取）。

.git
.gitignore
.env
.env.*
!.env.example

# Go 构建产物
*.exe
*.test
*.out
bin/

# 前端依赖与产物（镜像内会重新构建，见 Dockerfile 阶段 1）
web/node_modules/
web/dist/

# 运行时数据（向量库落盘，见 docs/memory.md §5.2）
data/
*.db
*.sqlite

# 文档与编辑器配置不参与构建
docs/
.github/
.vscode/
.idea/
.DS_Store
*.md
!README.md
